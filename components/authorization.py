# import sys
# sys.path.append("..")

# from apis.authorization_api import AuthorizationApi

# class Authorization:
#     def __init__(self, config):
#         self.api = AuthorizationApi(config)

#     def authorize_dashboard_user(self, service_id, user_id):
#         return self.api.authorize_dashboard_user(service_id, user_id)
  
#     def logout(self, service_id, user_id):
#         return self.api.logout_dashboard_user(service_id, user_id